# Environment variables for WordPress and deployment

# WordPress Database Configuration
MARIADB_ROOT_PASSWORD=your-root-password
MARIADB_DATABASE=fraueule_db
MARIADB_USER=wordpress
MARIADB_PASSWORD=your-db-password
WORDPRESS_DB_HOST=fraueule-db:3306
WORDPRESS_DB_USER=wordpress
WORDPRESS_DB_PASSWORD=your-db-password
WORDPRESS_DB_NAME=fraueule_db

# User and Group IDs
PUID=1000
PGID=1000

# Git Repository Configuration
# Set your repository URL (HTTPS or SSH)
# For public repos: https://github.com/username/repo.git
# For private repos with token: https://TOKEN@github.com/username/repo.git
GIT_REPO_URL=https://github.com/yourusername/your-theme-repo.git
GIT_BRANCH=main

# If your theme is in a subfolder within the repository, specify the path
# Leave empty if theme files are at the repository root
# Examples: "themes/fraueule" or "wp-content/themes/fraueule" or "src"
GIT_THEME_SUBFOLDER=

# Webhook Security
# Generate a strong random secret: openssl rand -hex 32
WEBHOOK_SECRET=your-secret-token-change-this-to-something-secure
